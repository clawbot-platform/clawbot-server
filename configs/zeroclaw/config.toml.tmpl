default_provider = "${ZEROCLAW_PROVIDER}"
default_model = "${ZEROCLAW_DEFAULT_MODEL}"
default_temperature = 0.7

[gateway]
host = "0.0.0.0"
port = 3000
require_pairing = true
allow_public_bind = true
